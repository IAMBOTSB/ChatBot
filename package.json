
{
  "name": "bot",
  "version": "1.0.0",
  "description": "hello",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    
  },
  "postinstall": "npm run build",
"start": "node server.js"
   
}

